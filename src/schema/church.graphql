type ChurchQuery {
    getAll: Response
    getById(id: ID!): Response
}
type ChurchMutation {
    create(name: String!, address: String!): ResponseMutation
    update(id: ID!, name: String, address: String): ResponseMutation
    delete(id: ID!): ResponseMutation
}
type Church {
    id: ID
    name: String
    address: String
}

